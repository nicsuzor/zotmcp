# ZotMCP MCP Server Configuration
# Inherits shared infrastructure from config.yaml
# Used by the MCP server to access the Zotero library vector database

defaults:
  - config              # Inherit base configuration
  - db: deploy          # Use deploy for Docker, override with db=dev for local
  - _self_

# MCP server specific configuration
job: mcp_server
